{
  "name": "sls-random",
  "version": "1.0.0",
  "description": "A serverless service that returns a random number.",
  "main": "dist/index.js",
  "author": "Tom Medema",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -fr dist && tsc -b .",
    "test": "mocha -r ts-node/register -r tsconfig-paths/register \"test/**/*.ts\"",
    "package": "serverless package",
    "deploy": "yarn run test && yarn run deploy-base",
    "deploy-base": "serverless deploy"
  },
  "dependencies": {
    "@org/random": "^1.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.11",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.7.1",
    "aws-lambda": "^0.1.2",
    "mocha": "^5.2.0",
    "serverless": "^1.30.1",
    "serverless-plugin-scripts": "^1.0.2",
    "ts-node": "^7.0.1",
    "tsconfig-paths": "^3.5.0",
    "typescript": "^3.0.1"
  }
}
