{
  "name": "@org/types",
  "version": "1.0.0",
  "description": "type definitions for external modules without types",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -fr dist && tsc -b ."
  },
  "devDependencies": {
    "typescript": "^3.0.1"
  }
}
